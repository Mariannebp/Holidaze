import { Typography } from "@mui/material";
import React from "react";

function NewVenue() {
  return (
    <>
      <Typography variant="h1">New Venue</Typography>
      <Typography variant="h1">New Venue</Typography>
      <Typography variant="h1">New Venue</Typography>
      <Typography variant="h1">New Venue</Typography>
      <Typography variant="h1">New Venue</Typography>
      <Typography variant="h1">New Venue</Typography>
      <Typography variant="h1">New Venue</Typography>
      <Typography variant="h1">New Venue</Typography>
      <Typography variant="h1">New Venue</Typography>
      <Typography variant="h1">New Venue</Typography>
      <Typography variant="h1">New Venue</Typography>
      <Typography variant="h1">New Venue</Typography>
    </>

  )
}

export default NewVenue;